<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zifuji.cloud.server.business.module.edu.book.mapper.BookMapper">


    <select id="getShouYeTuiJian" resultType="java.util.Map">
        SELECT
            b.id AS id,
            b.b_name AS bName,
            b.b_auth AS bAuth,
            b.b_type AS bType,
            b.b_img AS bImg,
            s.id AS sId,
            s.s_name AS sName
        FROM
            z_bu_edu_book b
                LEFT JOIN z_bu_edu_book_section s ON b.id = s.b_id
                AND s.id = ( SELECT max( id ) FROM z_bu_edu_book_section WHERE b_id = b.id )
        LIMIT 0,6
    </select>
    <select id="getZuiXinXiaoShuo" resultType="java.util.Map">
        SELECT
            id AS id,
            b_name AS bName,
            b_auth AS bAuth,
            b_type AS bType
        FROM
            z_bu_edu_book
        ORDER BY
            id DESC
        LIMIT 0,
            16
    </select>
</mapper>